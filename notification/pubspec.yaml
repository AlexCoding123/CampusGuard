name: notification
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # SSE - real-time server connection
  flutter_client_sse: ^2.0.3

  # HTTP
  http: ^1.2.0

  # Video playback
  video_player: ^2.9.1
  chewie: ^1.8.5

  # Date formatting
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true