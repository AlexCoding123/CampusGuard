services:
  backend:
    image: amirrezamojtahedi/campusguard:latest
    volumes:
      - incidents:/app/incidents
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:?}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: production

volumes:
  incidents: